class Trip {
   final String title;
   final DateTime startDate; 
   final DateTime endDate;
   final double price; 
   final String car;
   
   Trip(this.title,this.startDate,this.endDate, this.price, this.car);

}